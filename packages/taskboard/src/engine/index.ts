/**
 * The board's arithmetic on its own: which lanes there are, which card sits
 * where, what each cell is called and whether a move into it is allowed. No
 * DOM, no timers.
 */
export * from './state';
export * from './types';
