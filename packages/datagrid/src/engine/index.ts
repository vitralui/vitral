/**
 * The table's engine: what to draw, worked out from what the table was told.
 * No DOM in it, so another renderer — or a test — can use the same answers.
 */
export * from './types';
export * from './state';
