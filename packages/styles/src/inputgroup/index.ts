import { defineStyle } from '../defineStyle';
import css from './inputgroup.css?raw';

export const inputgroupStyle = defineStyle({
    name: 'inputgroup',
    css,
    classes: {
        root: 'vt-inputgroup'
    }
});
