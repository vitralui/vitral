import { defineStyle } from '../defineStyle';
import css from './inputgroupaddon.css?raw';

export const inputgroupaddonStyle = defineStyle({
    name: 'inputgroupaddon',
    css,
    classes: {
        root: 'vt-inputgroupaddon'
    }
});
