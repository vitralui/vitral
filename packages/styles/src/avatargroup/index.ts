import { defineStyle } from '../defineStyle';
import css from './avatargroup.css?raw';

export const avatargroupStyle = defineStyle({
    name: 'avatargroup',
    css,
    classes: {
        root: 'vt-avatargroup'
    }
});
