// The style contract lives in core, with no framework in it, so a renderer that
// does not depend on this package (the framework-free chart) shares it too.
export { classOf, defineStyle, type ClassEntry, type ComponentStyle } from '@vitral/core';
