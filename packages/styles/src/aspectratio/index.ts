import { defineStyle } from '../defineStyle';
import css from './aspectratio.css?raw';

export const aspectratioStyle = defineStyle({ name: 'aspectratio', css, classes: { root: 'vt-aspectratio' } });
