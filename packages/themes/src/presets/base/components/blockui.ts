// The veil over a region that is busy: the mask colour, the region's corners.
export default {
    root: {
        borderRadius: '{content.borderRadius}',
        background: '{mask.background}',
        color: '{mask.color}',
        transitionDuration: '{transitionDuration}'
    }
};
