// Buttons joined into one control: shared edges, rounded only at the ends.
export default {
    root: {
        borderRadius: '{formField.borderRadius}',
        /** The hairline between two filled buttons. */
        dividerColor: 'color-mix(in srgb, currentColor 22%, transparent)'
    }
};
