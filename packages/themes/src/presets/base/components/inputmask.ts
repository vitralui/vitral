// A masked box is a text box: the field chrome does the drawing. Its own token
// is how the digits are set, so the slots line up column by column as they fill.
export default {
    root: {
        fontVariantNumeric: 'tabular-nums'
    }
};
