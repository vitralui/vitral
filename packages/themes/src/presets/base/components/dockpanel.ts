// A DockPanel usually frames a whole window (toolbar, status bar, side panes)
// whose regions meet edge to edge, so it has no spacing unless asked for.
export default {
    root: {
        spacing: '0'
    }
};
