// The addon's own tokens live with InputGroup; this file exists so the class
// map and the component have a namespace of their own to be themed through.
export default {
    root: {
        background: '{inputgroup.addon.background}',
        borderColor: '{inputgroup.addon.borderColor}',
        color: '{inputgroup.addon.color}',
        paddingX: '{inputgroup.addon.paddingX}',
        minWidth: '{inputgroup.addon.minWidth}'
    }
};
