// A badge pinned to the corner of what it wraps, cut out from it by a ring in
// the surface colour.
export default {
    root: {
        outlineWidth: '2px',
        outlineColor: '{overlay.modal.background}'
    }
};
