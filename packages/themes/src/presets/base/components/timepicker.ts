// A text box that takes typed times, and a list of them under it. The box and
// its button are the shared form field, so only the list needs tokens.
export default {
    panel: {
        minWidth: '9rem'
    },
    list: {
        // About eight rows, so the list reads as a list rather than as a page.
        height: '15rem'
    }
};
