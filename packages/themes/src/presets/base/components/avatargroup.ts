// Avatars overlapping in a row, each carrying a ring of the surface it sits on
// so the stack reads as separate faces rather than one shape.
export default {
    root: {
        offset: '-0.625rem',
        borderWidth: '2px',
        borderColor: '{content.background}'
    }
};
