// The grid spaces rows and columns separately and counts both from 0.
// The lines are the `showGridLines` debugging aid.
export default {
    root: {
        rowSpacing: '0',
        columnSpacing: '0'
    },
    line: {
        width: '1px',
        style: 'dashed',
        color: '{primary.color}'
    }
};
