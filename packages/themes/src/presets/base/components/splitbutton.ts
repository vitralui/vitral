// A button and its menu button, welded; the menu button is narrower and set
// off by a hairline.
export default {
    root: {
        borderRadius: '{formField.borderRadius}',
        roundedBorderRadius: '{borderRadius.pill}',
        dividerColor: 'color-mix(in srgb, currentColor 22%, transparent)',
        dropdownWidth: '2.25rem'
    }
};
