// A stack panel has no spacing unless asked; a web page almost always
// wants some, so the default is a small one. `spacing` on the instance wins.
export default {
    root: {
        spacing: '0.5rem'
    }
};
