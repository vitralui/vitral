import { definePreset } from '../engine/preset';
import { Base } from './base';

/**
 * The web-native look: Inter, zinc surfaces, a blue primary, soft halos on
 * focus. The preset every other one is a diff against.
 */
export const Prism = definePreset(Base, {});
